# Normalise line endings to LF in the repository, and force LF in the working
# tree too (eol=lf) so Windows checkouts stay consistent and git status keeps
# clean across platforms.
* text=auto eol=lf
